--- jemalloc.orig/configure
+++ jemalloc/configure
@@ -4447,6 +4447,11 @@
 
 	force_lazy_lock="1"
 	;;
+  *-*-kfreebsd*)
+	CFLAGS="$CFLAGS"
+	abi="elf"
+	$as_echo "#define JEMALLOC_PURGE_MADVISE_FREE  " >>confdefs.h
+	;;
   *-*-linux*)
 	CFLAGS="$CFLAGS"
 	CPPFLAGS="$CPPFLAGS -D_GNU_SOURCE"
